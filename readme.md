# Express Logger - Production WAL + Kafka Pipeline

Production-ready Express logging system with crash-safe Write-Ahead Log (WAL) and separate Kafka shipper process.

## Architecture

Two independent Node.js processes: